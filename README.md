# Local-Quiz-Chatbot
A fully offline chatbot that generates multiple-choice quizzes from an uploaded Word (.docx) or PDF (.pdf) document, using a local Ollama model. No cloud API calls, no API keys — everything runs on your machine.
